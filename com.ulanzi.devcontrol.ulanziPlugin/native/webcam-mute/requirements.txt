# webcam-mute – Python dependencies
# Install: pip install -r requirements.txt
#
# Virtual camera backend: Unity Capture (no OBS required)
# The installer (install.ps1) downloads and registers the DLL automatically.
# Manual: https://github.com/schellingb/UnityCapture
# Teams camera name: "Unity Video Capture"

opencv-python>=4.9.0        # webcam capture + frame processing
numpy>=1.26.0               # black-frame generation
pyvirtualcam>=0.10.0        # virtual camera output (Unity Capture backend)
fastapi>=0.111.0            # HTTP control API
uvicorn[standard]>=0.29.0   # ASGI server for FastAPI
pygrabber>=0.2              # optional: camera name detection on Windows
